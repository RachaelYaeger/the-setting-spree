<% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), :itemprop => "image", class: 'show-product-image' %>
<% else %>
  <%= product_image(@product, :itemprop => "image", class: 'show-product-image') %>
<% end %>